:
# compile 64-bit arm version (aarch64)
# first install the compiler:
# sudo apt install gcc gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu
aarch64-linux-gnu-gcc -I. *.c -o mosquitto_simplepub -pthread -static
